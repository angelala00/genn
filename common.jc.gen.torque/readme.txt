torque-generator-jc-3.3.jar
是根据3.3版本的源码上做了些调整，有些不合理的地方改了一下，
在改的时候，只是针对我自己遇到的情况做的调整
所以后面还会慢慢完善

改完之后生成的schema以及Java代码是可以直接用的 ：）

今天加了一个 jc_replace-java 的ant命令这个命令在执行完jc_om之后执行它，会把关键字替换为正确的写法
.desc" .`desc`"
如有其它关键字冲突，可以自行添加
